%p#notice= notice
%h1 Listing Pets
%table
  %thead
    %tr
      %th Name
      %th Birthday
      %th Gender
      %th Image
      %th{:colspan => "3"}
  %tbody
    - @pets.each do |pet|
      %tr
        %td= pet.name
        %td= pet.birthday
        %td= pet.gender.try(:name)
        %td= pet.species.try(:name)
        %td= pet.image_id
        %td= link_to 'Show', pet
        - if pet.user_verified?(current_user)
          %td= link_to 'Edit', edit_pet_path(pet)
          %td= link_to 'Destroy', pet, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Pet', new_pet_path